# Local Installation

This section describes how to install **MSight** locally for development or production use.

---

## 🧰 Prerequisites

Before installing, ensure the following dependencies are available on your system:

- **Python 3.10 or later**
- **pip** (Python package manager)
- **Git** (optional, for cloning the repository)
- (Optional) **virtual environment** tool such as `venv` or `conda` for isolated installs

---

## 💻 Installation Steps

### 1. Clone the Repository

If you haven’t already, clone the MSight repository from GitHub:

```bash
git clone https://github.com/your-org/msight.git
cd msight

---

Next: [Docker Installation →](docker.md)